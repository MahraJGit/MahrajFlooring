/**
 * Legacy static services content kept only as a thin compatibility shim.
 * Live public data comes from `@/lib/public/services`.
 */
export type {
  MegaMenuColumn,
  ServiceCard as Service,
  ServiceDetailView as ServiceDetail,
} from "@/lib/public/services";
